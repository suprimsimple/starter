{% extends 'layouts/base' %}

{% block content %}
	{% import "macros" as macros %}
		<div class="grid place-content-center mt-6">
			<div class="p-16 m-0 shadow-lg bg-gray-50 rounded-md bg-slate-600  text-white">
				<h1 class="heading-1">Hello sdfsd asdas lolo👋</h1>


				 React App Down Below Show App </br>	
				<h2 class="heading-3 mt-4">{{ craft.vite.devServerRunning ? "Dev server running 🏃🏽‍♀️" : "Dev server missing 🤷‍♂️" }}</h2>
			</div>
		</div>
	

		{% set params = [ {'bedrooms' : craft.app.request.getParam('bedrooms')|default} ] %}


		<div id="root" data-params="{{ params|json_encode }}"></div>

		{{ craft.vite.script("dist/index.js") }}
</script>
{% endblock %}


